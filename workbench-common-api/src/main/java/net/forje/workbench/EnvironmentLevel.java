package net.forje.workbench;

/**
 * Defines the set of environment levels.  Used to quantify environments and the levels which a module may be run in
 */
public enum EnvironmentLevel {
    All, Production, Demo, UAT, Staging, QA, Dev
}
